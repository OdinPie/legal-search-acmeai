import React from 'react';
import C
const DisplayCards = ({value = []}) => {

    return (
        <div>
            {value && value.map(doc)=> <Card}
        </div>
    );
};

export default DisplayCards;